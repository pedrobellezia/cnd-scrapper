services:
  app:
    build: .
    container_name: cnd-scrapper
    ports:
      - "5049:5049"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./screenshots:/app/screenshots

